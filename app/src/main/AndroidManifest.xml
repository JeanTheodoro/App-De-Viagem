<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.aluratravel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".aluratravel.ui.activity.ResumoCompraActivity"></activity>
        <activity android:name=".aluratravel.ui.activity.Pagamento_Activity" />
        <activity android:name=".aluratravel.ui.activity.ResumoPacoteActivity" />
        <activity android:name=".aluratravel.ui.activity.ListaPacotesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>